# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. 
on:
  # Triggers the workflow on push or pull request events but only for the master branch
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
#      - name: Run a multi-line script
#        run: |
#          echo bc98c37f-5905-43a0-8069-4de5cb82c45a | docker login --username=israelbuiatti@gmail.com registry.heroku.com --password-stdin
#          docker build --tag registry.heroku.com/buiatti-ms-backend/web ./backend
#          docker push registry.heroku.com/buiatti-ms-backend/web
#          heroku container:release web --app buiatti-ms-backend --recursive
          
          

      - name: Deploy to Heroku
        # You may pin to the exact commit or the version.
        # uses: AkhileshNS/heroku-deploy@79ef2ae4ff9b897010907016b268fd0f88561820
        uses: AkhileshNS/heroku-deploy@v3.12.12
        with:
          # This will be used for authentication. You can find it in your heroku homepage account settings
          heroku_api_key: bc98c37f-5905-43a0-8069-4de5cb82c45a
          # Email that you use with heroku
          heroku_email: israelbuiatti@gmail.com
          # The appname to use for deploying/updating
          heroku_app_name: buiatti-ms-backend2
          usedocker: true
          appdir: backend
          
